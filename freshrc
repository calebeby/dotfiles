# vim: set ft=sh:

fresh freshshell/fresh bin/fresh --bin

# utils
fresh pengwynn/dotfiles bin/termcolors --bin
fresh twe4ked/dotfiles bin/ghu --bin

# prompt
fresh sindresorhus/pure async.zsh
fresh sindresorhus/pure pure.zsh

# completions

fresh zsh-users/zsh-completions src --file=completion/
fresh freshshell/fresh contrib/completion/fresh-completion.zsh --file=completion/_fresh

# custom
fresh vimrc --file=~/.config/nvim/init.vim
fresh zshrc --file=~/.zshrc
