[
  (section)
  (code_block)
  (raw_block)
  (div)
] @fold

(list_item
  content: (list_item_content (_)) @fold)

(list_item
  term: (_)) @fold

;; list_item with non-empty content
; ((list_item
;   content: (list_item_content (_) @child @fold)))

; ;; list_item with a non-empty definition
; ((list_item
;   definition: (definition (_) @child))) @fold
