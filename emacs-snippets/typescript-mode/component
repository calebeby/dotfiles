# -*- mode: snippet -*-
# name: component
# key: co
# --

import { Component, h } from 'preact'

interface ${2:Props} {
  ${3:props}
}

interface ${4:State} {
  ${5:state}
}

export default class ${1:ClassName} extends Component<$2, $4> {
  constructor() {
    super()
    this.state = {${6:$5}}
  }

  render({${7:}}: $2, {${8:}}: $4) {
    return ${9:<h1>Hi</h1>}
  }
}